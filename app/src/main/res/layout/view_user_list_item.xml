<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="userModel"
            type="com.shahrukhamd.githubuser.data.model.GithubUser" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageView
            android:id="@+id/user_item_image"
            circleCropEnabled="@{true}"
            loadImage="@{userModel.avatarUrl}"
            placeholder="@{@drawable/ic_user_default_48px}"
            android:layout_width="@dimen/main_list_user_icon_width_height"
            android:layout_height="@dimen/main_list_user_icon_width_height"
            android:src="@drawable/ic_user_default_48px"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/user_item_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:ellipsize="end"
            android:fontFamily="sans-serif-medium"
            android:maxLines="2"
            android:text="@{userModel.login}"
            android:textSize="20sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.488"
            app:layout_constraintStart_toEndOf="@+id/user_item_image"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="@tools:sample/full_names" />

        <TextView
            android:id="@+id/user_item_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{userModel.id.toString()}"
            app:layout_constraintBottom_toBottomOf="@+id/user_item_image"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="123345" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>